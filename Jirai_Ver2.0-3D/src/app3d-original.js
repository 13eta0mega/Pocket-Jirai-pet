import * as THREE from 'three';
import { FBXLoader } from 'three/addons/loaders/FBXLoader.js';
import { OrbitControls } from 'three/addons/controls/OrbitControls.js';
const $=s=>document.querySelector(s);const ui={canvas:$('#gl'),fps:$('#fps'),meshInfo:$('#meshInfo'),status:$('#status'),modelName:$('#modelName'),fileInput:$('#fileInput'),pickBtn:$('#pickBtn'),restoreBtn:$('#restoreBtn'),empty:$('#emptyState'),dropZone:$('#dropZone'),reset:$('#resetCamera'),idle:$('#idleToggle'),grid:$('#gridToggle'),vertex:$('#vertexCount'),tri:$('#triCount'),meshes:$('#meshCount'),badge:$('#diagnosticBadge'),skin:$('#skinInfo'),anim:$('#animInfo'),material:$('#materialInfo'),texture:$('#textureInfo'),size:$('#sizeInfo')};
const renderer=new THREE.WebGLRenderer({canvas:ui.canvas,antialias:true,alpha:false,powerPreference:'high-performance'});renderer.setPixelRatio(Math.min(devicePixelRatio||1,2));renderer.outputColorSpace=THREE.SRGBColorSpace;renderer.toneMapping=THREE.ACESFilmicToneMapping;renderer.toneMappingExposure=1;renderer.shadowMap.enabled=false;renderer.setClearColor(0x100c14,1);const scene=new THREE.Scene();scene.background=new THREE.Color(0x100c14);const camera=new THREE.PerspectiveCamera(32,1,.001,1000);camera.position.set(0,.15,4);const controls=new OrbitControls(camera,renderer.domElement);controls.enableDamping=true;controls.dampingFactor=.07;controls.screenSpacePanning=true;controls.target.set(0,0,0);controls.minDistance=.2;controls.maxDistance=20;scene.add(new THREE.HemisphereLight(0xffe7f3,0x2a2132,2.25));const key=new THREE.DirectionalLight(0xffffff,3.2);key.position.set(-3,4,5);scene.add(key);const fill=new THREE.DirectionalLight(0xff8fc1,1.2);fill.position.set(3,1,2);scene.add(fill);const rim=new THREE.DirectionalLight(0x98bfff,.85);rim.position.set(1,2,-4);scene.add(rim);const grid=new THREE.GridHelper(4,20,0x56374a,0x2c2231);grid.visible=false;scene.add(grid);const root=new THREE.Group();scene.add(root);let model=null,baseY=0,last=performance.now(),frames=[],sourceBytes=0;
function formatBytes(n){if(!n)return '-';const u=['B','KB','MB','GB'];let i=0,v=n;while(v>=1024&&i<u.length-1){v/=1024;i++;}return `${v.toFixed(i?2:0)} ${u[i]}`;}function disposeObject(obj){obj.traverse(o=>{if(o.geometry)o.geometry.dispose?.();const mats=Array.isArray(o.material)?o.material:[o.material];for(const m of mats){if(!m)continue;for(const k of Object.keys(m)){const v=m[k];if(v?.isTexture)v.dispose?.();}m.dispose?.();}});}function setMaterialQuality(m){if(!m)return;m.side=THREE.FrontSide;m.transparent=!!m.transparent;m.alphaTest=m.alphaTest||0;if(m.map){m.map.colorSpace=THREE.SRGBColorSpace;m.map.anisotropy=Math.min(8,renderer.capabilities.getMaxAnisotropy());m.map.needsUpdate=true;}if(m.emissiveMap)m.emissiveMap.colorSpace=THREE.SRGBColorSpace;m.needsUpdate=true;}
function analyze(obj,file){let vertices=0,triangles=0,meshes=0,skinned=0,materials=new Set(),textures=new Set();obj.traverse(o=>{if(!o.isMesh)return;meshes++;if(o.isSkinnedMesh)skinned++;const g=o.geometry;if(g){const pos=g.getAttribute('position');if(pos)vertices+=pos.count;if(g.index)triangles+=Math.floor(g.index.count/3);else if(pos)triangles+=Math.floor(pos.count/3);}const ms=Array.isArray(o.material)?o.material:[o.material];for(const m of ms){if(!m)continue;materials.add(m);setMaterialQuality(m);for(const k of Object.keys(m)){const v=m[k];if(v?.isTexture)textures.add(v);}}});const clips=obj.animations?.length||0;ui.vertex.textContent=vertices.toLocaleString();ui.tri.textContent=triangles.toLocaleString();ui.meshes.textContent=meshes.toLocaleString();ui.meshInfo.textContent=`${vertices.toLocaleString()} V / ${triangles.toLocaleString()} T`;ui.skin.textContent=skinned?`${skinned} SkinnedMesh`:'없음';ui.anim.textContent=`${clips} clips`;ui.material.textContent=materials.size.toLocaleString();ui.texture.textContent=textures.size.toLocaleString();ui.size.textContent=formatBytes(file?.size||sourceBytes);ui.badge.textContent=skinned||clips?'Rig data 있음':'Static mesh';return{vertices,triangles,meshes,skinned,materials:materials.size,textures:textures.size,clips};}
function fitModel(obj){obj.updateMatrixWorld(true);const box=new THREE.Box3().setFromObject(obj),size=new THREE.Vector3(),center=new THREE.Vector3();box.getSize(size);box.getCenter(center);obj.position.sub(center);obj.updateMatrixWorld(true);const box2=new THREE.Box3().setFromObject(obj),size2=new THREE.Vector3();box2.getSize(size2);baseY=-box2.min.y;obj.position.y+=baseY;const maxDim=Math.max(size2.x,size2.y,size2.z)||1;grid.scale.setScalar(Math.max(.2,maxDim/2));grid.position.y=0;controls.target.set(0,size2.y*.47,0);const fov=THREE.MathUtils.degToRad(camera.fov),distance=(size2.y*.62)/Math.tan(fov/2);camera.position.set(size2.x*.15,size2.y*.49,Math.max(distance,maxDim*1.35));camera.near=Math.max(.001,maxDim/1000);camera.far=Math.max(100,maxDim*80);camera.updateProjectionMatrix();controls.minDistance=maxDim*.28;controls.maxDistance=maxDim*8;controls.update();}
async function cacheFile(file){try{const db=await openDB();await new Promise((resolve,reject)=>{const tx=db.transaction('files','readwrite');tx.objectStore('files').put(file,'last-fbx');tx.oncomplete=resolve;tx.onerror=()=>reject(tx.error);});ui.restoreBtn.hidden=false;}catch(e){console.warn('FBX cache skipped',e);}}function openDB(){return new Promise((resolve,reject)=>{const r=indexedDB.open('jirai-original-fbx',1);r.onupgradeneeded=()=>r.result.createObjectStore('files');r.onsuccess=()=>resolve(r.result);r.onerror=()=>reject(r.error);});}async function restoreCached(){try{const db=await openDB();return await new Promise((resolve,reject)=>{const tx=db.transaction('files');const r=tx.objectStore('files').get('last-fbx');r.onsuccess=()=>resolve(r.result||null);r.onerror=()=>reject(r.error);});}catch{return null;}}
async function loadFBXFile(file,{cache=true}={}){if(!file)return;if(!/\.fbx$/i.test(file.name||'')){ui.status.textContent='FBX 파일만 선택해 주세요';return;}ui.status.textContent='원본 FBX 읽는 중…';ui.empty.classList.remove('hidden');const reader=await file.arrayBuffer();sourceBytes=file.size;ui.status.textContent='FBX 원본 파싱 중…';await new Promise(r=>requestAnimationFrame(r));try{const loader=new FBXLoader(),obj=loader.parse(reader,'');if(model){root.remove(model);disposeObject(model);}model=obj;root.add(model);model.rotation.set(0,0,0);model.scale.setScalar(1);const info=analyze(model,file);fitModel(model);ui.modelName.textContent=file.name;ui.status.textContent='원본 FBX 렌더링 준비 완료';ui.empty.classList.add('hidden');if(cache)cacheFile(file);window.__JIRAI3D_QA__={ready:true,source:'direct-original-fbx',fileName:file.name,fileSize:file.size,...info,noDecimation:true,pseudoRig:false};}catch(e){console.error(e);ui.status.textContent=`FBX 파싱 실패: ${e.message}`;window.__JIRAI3D_QA__={ready:false,error:String(e),source:'direct-original-fbx'};}}
ui.fileInput.addEventListener('change',()=>loadFBXFile(ui.fileInput.files?.[0]));ui.pickBtn.addEventListener('click',()=>ui.fileInput.click());ui.reset.addEventListener('click',()=>model&&fitModel(model));ui.grid.addEventListener('change',()=>grid.visible=ui.grid.checked);ui.restoreBtn.addEventListener('click',async()=>{const f=await restoreCached();if(f)loadFBXFile(f,{cache:false});});for(const type of ['dragenter','dragover'])ui.dropZone.addEventListener(type,e=>{e.preventDefault();ui.dropZone.classList.add('dragover');});for(const type of ['dragleave','drop'])ui.dropZone.addEventListener(type,e=>{e.preventDefault();ui.dropZone.classList.remove('dragover');});ui.dropZone.addEventListener('drop',e=>{const f=[...e.dataTransfer.files].find(x=>/\.fbx$/i.test(x.name));if(f)loadFBXFile(f);});function resize(){const r=ui.canvas.getBoundingClientRect(),w=Math.max(2,Math.floor(r.width)),h=Math.max(2,Math.floor(r.height));renderer.setSize(w,h,false);camera.aspect=w/h;camera.updateProjectionMatrix();}function loop(now){requestAnimationFrame(loop);resize();last=now;controls.update();if(model&&ui.idle.checked){const t=now*.001;model.position.y=baseY+Math.sin(t*1.15)*.0028;model.rotation.z=Math.sin(t*.55)*.004;}else if(model){model.position.y=baseY;model.rotation.z=0;}renderer.render(scene,camera);frames.push(now);while(frames.length&&frames[0]<now-1000)frames.shift();ui.fps.textContent=`${frames.length} FPS`;}restoreCached().then(f=>{if(f){ui.restoreBtn.hidden=false;ui.status.textContent='원본 FBX 선택 또는 마지막 파일 복원 가능';}});requestAnimationFrame(loop);
